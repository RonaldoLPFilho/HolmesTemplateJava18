environment.name=test

server.port=9290
server.ssl.enabled=false
server.ssl.key-store=${HOME}/keys/private-key.jks
server.ssl.key-store-password=despegar

server.jetty.context-path=/

# Nebula contract
logging.config=/service/config/logback.xml

xclient.validation.enabled=false
xclient.validation.ignored.paths=/swagger-ui,/v3/api-docs